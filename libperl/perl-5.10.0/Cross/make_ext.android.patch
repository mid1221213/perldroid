--- ../ext/util/make_ext	2010-04-04 01:02:52.240380377 +0200
+++ make_ext.android	2010-04-04 00:45:46.309130549 +0200
@@ -96,7 +96,7 @@
 # 'dynamic', 'static', and 'static_pic' (the last one respects
 # CCCDLFLAGS such as -fPIC -- see static_target in the main Makefile.SH)
 case "$target" in
-dynamic)    makeargs="LINKTYPE=dynamic";
+dynamic)    makeargs="LINKTYPE=dynamic PERL_ARCHIVE=/data/data/org.gtmp.perl/lib/libperl.so";
 	    target=all
 	    ;;
 static)     makeargs="LINKTYPE=static CCCDLFLAGS="
@@ -126,7 +126,8 @@
 esac
 
 if test ! -f $makefile ; then
-	test -f Makefile.PL && $run ../$depth/miniperl -I../$depth/lib Makefile.PL INSTALLDIRS=perl INSTALLMAN3DIR=none PERL_CORE=1 $passthru
+#	test -f Makefile.PL && $run ../$depth/miniperl -I../$depth/lib Makefile.PL INSTALLDIRS=perl INSTALLMAN3DIR=none PERL_CORE=1 $passthru
+	test -f Makefile.PL && $run ../$depth/miniperl -I../$depth/fake_config_library -MConfig Makefile.PL INSTALLDIRS=perl INSTALLMAN3DIR=none PERL_CORE=1 $passthru CC=$HOME/android-ndk/build/prebuilt/linux-x86/arm-eabi-4.4.0/bin/arm-eabi-gcc LD=$HOME/android-ndk/build/prebuilt/linux-x86/arm-eabi-4.4.0/bin/arm-eabi-gcc PERLRUN="/usr/bin/perl -I../$depth/fake_config_library -MConfig" FULLPERLRUN="/usr/bin/perl -I../$depth/fake_config_library -MConfig" NOECHO= MKPATH="mkdir -p" PERL="/usr/bin/perl -I/usr/lib/perl/5.10.0" ABSPERLRUN="/usr/bin/perl -I/usr/lib/perl/5.10.0"
 fi
 if test ! -f $makefile ; then
 	echo "Warning: No Makefile!"
