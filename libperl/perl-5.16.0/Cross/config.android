OS = linux-androideabi

# ARCH: can be one of arm i686
ARCH = arm

PERL_VERSION = 5.16.0

# ANDROID_VERSION: API version of Android OS
ANDROID_VERSION = 15

DIR_PATH = $(ARCH)-$(PERL_VERSION)-$(ANDROID_VERSION)

PERL_PATH = $(HOME)/hostperl-$(PERL_VERSION)
